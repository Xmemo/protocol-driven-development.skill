# Stage -1 - PRD Intake / Input Extraction

这一份不是让用户从空白开始写。

这一份是让 AI 先把项目里已经存在的输入抓出来，再给用户确认。

## 1. Scope Level

- [ ] `app-level`
- [ ] `feature-level`

## 2. 输入来源清单

| SOURCE_ID | 文件路径 | 类型 | 可信度 | 用途 |
| --- | --- | --- | --- | --- |
| `SRC_001` | [填写] | [PRD/ideas/handoff/design/code] | [high/medium/low] | [填写] |

## 3. 已提取事实

| FACT_ID | 事实 | 来自哪个 SOURCE_ID |
| --- | --- | --- |
| `FACT_001` | [填写] | `SRC_001` |

## 4. 冲突与歧义

| CONFLICT_ID | 冲突点 | 来源 | 建议处理 |
| --- | --- | --- | --- |
| `CONFLICT_001` | [填写] | [填写] | [填写] |

## 5. 当前假设

| ASSUMPTION_ID | 假设 | 为什么需要先假设 |
| --- | --- | --- |
| `ASSUME_001` | [填写] | [填写] |

## 6. 待确认草稿

这里不贴空模板，贴 AI 根据来源整理后的 Stage 0 草稿。

```md
[AI 在这里放规范化后的 Stage 0 草稿]
```

## 7. 给 AI 的硬规则

- 如果项目里已经有 PRD、开发文档、设计规范或现有代码，不得直接让用户从空白开始填
- `事实`、`冲突`、`假设` 必须分开写
- 如果没有找到可用输入，才允许退回空模板模式
